# Models

Learned model weights / checkpoints can live here (e.g. downloaded OCR or
embedding models). 

This directory is only used when a provider adapter needs a local model. In the
default mock mode and with cloud providers, no local weights are required.

`.gitignore`-friendly: do not commit large model binaries to the repository.